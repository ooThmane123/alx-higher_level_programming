#!/usr/bin/node

if (process.argv.length >= 2) {
  console.log(parseInt(process.argv[2]) + parseInt(process.argv[3]));
} else {
  console.log(parseInt(process.argv[2]) + parseInt(process.argv[3]));
}
